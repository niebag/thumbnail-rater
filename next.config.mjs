/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: 'colorful-lapwing-895.convex.site',
            },
        ],
    },
};

export default nextConfig;
